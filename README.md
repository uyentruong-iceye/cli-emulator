# CLI emulator

Starting server

```
virtualenv cli-emulator
source cli-emulator/bin/activate
cd xterm-server
pip install -r requirements.txt
python main.py
```

Starting client

```
cd xterm-front
npm install
npm start
```
